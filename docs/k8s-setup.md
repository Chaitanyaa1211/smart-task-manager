on k8s-master ec2 instance installed k8s by following official k8s installation documents , installed kubectl , kubeadm , kubelet and calico as network plugin and applied configured kubectl , and bootstrapped the cluster with kubeadm init creating the controler plane components

followed the same procedure to install k8s on k8s-worker ec2 instance and joined the node with cluster


